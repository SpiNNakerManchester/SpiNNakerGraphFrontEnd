// open tensorboard command from anaconda. First navigate to the TensorSample dir then hit the command.

tensorboard --logdir .




// enable debugger
tensorboard --logdir /tmp/logdir --debugger_port 7000